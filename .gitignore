node_modules/
build/
coverage/
# NOTE: dist/ is intentionally committed so Hostinger's git auto-pull deploys
# the built frontend (there is no server-side build step). Rebuild + commit
# dist/ after any frontend change: npm run build
.DS_Store
*.log
.env*
!.env.example
firebase-applet-config.json

# Generated outreach drafts (contain lead emails)
scripts/attorney-outreach/_drafts/
